machine:
  timezone:
    Australia/Perth

  php:
    version: 5.4.5

dependencies:
  post:
    # Update our dependencies
    - composer install

test:
  override:
     # Run behat
    - bin/behat

    # Run PHPCS
    - php bin/phpcs --standard=Ecg

    # Run Unit Tests
    #- php bin/phpunit