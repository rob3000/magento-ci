machine:
  timezone:
    Australia/Perth

  php:
    version: 5.4.4

dependencies:
  post:
    # Update our dependencies
    - composer install

test:
  override:
     # Run behat
    - bin/behat

    # Run PHPCS
    - php bin/phpspec run

    # Run Unit Tests
    #- php bin/phpunit